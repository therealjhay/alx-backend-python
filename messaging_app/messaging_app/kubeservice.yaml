apiVersion: v1
kind: Service
metadata:
  name: messaging-service
spec:
  selector:
    app: messaging-app
    version: blue  # Change this to 'green' to switch traffic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer